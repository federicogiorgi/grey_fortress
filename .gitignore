.godot/
export_presets.cfg
*.translation

# Build outputs stay local and runnable, but are never committed:
# distribution happens through GitHub Releases (see the README's
# "Exporting for Windows"). The raw exe brushes GitHub's 100 MB
# per-file limit, and every committed export would bloat the git
# history forever.
Grey Fortress.exe
Grey Fortress.console.exe
Grey Fortress.pck
Grey Fortress-win64.zip
